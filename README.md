# webapp_cloudcomputing
# Scalable Web App (Opinionated Defaults)

This repo is a starter scaffold for a scalable web application using the chosen defaults:
- Next.js (React) full‑stack app
- PostgreSQL (Prisma) for relational storage
- Redis for caching
- Docker + docker-compose for local development
- Kubernetes manifests for production
- Terraform skeleton for infra provisioning
- GitHub Actions workflow template for CI/CD



